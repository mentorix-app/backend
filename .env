# Local defaults for `make run` / `make dev`, committed to git. No secrets here.
# Personal values and secrets live in .env.local (git-ignored, created by `make setup`).
# Precedence: shell environment > .env.local > .env. Canon of keys: internal/config/config.go.
# Render stage takes everything from the Dashboard (render.yaml `sync: false`), not from these files.

APP_ENV=development
PORT=8080

# Local Postgres (dev credentials app:app); mentorix_test is used by integration tests
DATABASE_URL=postgres://app:app@localhost:5432/mentorix?sslmode=disable
TEST_DATABASE_URL=postgres://app:app@localhost:5432/mentorix_test?sslmode=disable
REDIS_URL=redis://localhost:6379/0

# Local frontend origins
CORS_ALLOW_ORIGINS=http://localhost:5173,http://localhost:3000
TRUSTED_PROXY_CIDRS=

ACCESS_TTL_MINUTES=15
REFRESH_TTL_DAYS=30
REFRESH_COOKIE_NAME=mentorix_refresh
REFRESH_COOKIE_PATH=/auth
REFRESH_COOKIE_DOMAIN=
REFRESH_COOKIE_SAMESITE=lax
REFRESH_COOKIE_SECURE=false

AUTH_LOGIN_RATE_MAX=20
AUTH_LOGIN_RATE_WINDOW_SEC=900
AUTH_REGISTER_RATE_MAX=10
AUTH_REGISTER_RATE_WINDOW_SEC=900

TRAINER_INVITE_TTL_DAYS=7
